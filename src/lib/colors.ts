/**
 * The fallback accent for a goal with no category. Uncategorized is deliberately
 * its own quiet hue — not one of the user's category colors, and clearly distinct
 * from the missed-day grey (#e5e7eb) — so an uncategorized goal still reads as a
 * live row, not an inactive one. Single source for the few fallbacks that need it.
 */
export const UNCATEGORIZED_COLOR = "#bd8a9c";
